import { createStore } from 'vuex'
import announcement from './modules/announcement'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    announcement
  }
})
